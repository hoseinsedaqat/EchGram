export interface directState {
    noneDirect: boolean,
    directOne: boolean,
    // eslint-disable-next-line
    allDirects:{chats:any, whichDirect:number, directImg:string, userName: string}[],
    // eslint-disable-next-line
    getEachDirect:any
}