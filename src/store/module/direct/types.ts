export interface directState {
    noneDirect: boolean,
    directOne: boolean,
    // eslint-disable-next-line
    allDirects:{chats:any,whichDirect:number}[],
    // eslint-disable-next-line
    getEachDirect:any
}